#ifndef __SERVER_H__
#define __SERVER_H__

#include "User.h"
#include "Define.h"

class server
{
  public:
    server(/* args */);
    ~server();

    // SA sock;
    // updatePerrList();
};

#endif // __SERVER_H__
